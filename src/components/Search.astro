---
// ================================================================
// Search — Pagefind 搜索框组件（临时禁用JS，先上线）
// 后续再修复搜索功能
// ----------------------------------------------------------------
---

<div class="search-wrapper">
  <div class="search-input-group">
    <svg class="search-input-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/>
    </svg>
    <input
      type="text"
      class="search-input"
      placeholder="搜索功能正在维护..."
      aria-label="搜索"
      disabled
    />
  </div>
</div>

<style>
  .search-wrapper {
    max-width: 720px;
    margin: 0 auto;
    width: 100%;
  }
  .search-input-group {
    position: relative;
    display: flex;
    align-items: center;
  }
  .search-input-icon {
    position: absolute;
    left: 1rem;
    color: var(--text-muted);
    pointer-events: none;
  }
  .search-input {
    width: 100%;
    padding: 0.85rem 1rem 0.85rem 2.8rem;
    font-size: 1rem;
    border: 2px solid var(--border-color);
    border-radius: 10px;
    background: var(--bg-primary);
    color: var(--text-primary);
    outline: none;
  }
</style>